#ifndef __CONST_H
#define __CONST_H
#include <string>

const int L = 3;// number of levels is L+1
const int initial_child_counts = 4; //
const int MAX_TOPICS = 1 + 4 + 16 + 64;
const std::string dump_dir = "../dump/";

const int BlockDim = 256;
const int GridDim = 256;

#endif